---
title: 'RAG Testing: Grade the Layers, Not the Box'
description: >-
  A fluent answer hides a broken retriever, so you do not test a RAG pipeline as
  one box. Test each layer: retrieval, reranking, grounded versus true, graph
  traversal, and the poisoned document. Then test the parts that are not
  deterministic with classical tools.
kicker: GENAI_TESTING
topic: GenAI
ctaNote: >-
  Shipping a RAG or agentic system that retrieves before it answers? This is the
  layered suite I build so a fluent paragraph can never hide a broken retriever.
series:
  name: Evals Are Checks, Not Tests
  order: 7
  code: rebuild/v2
author: Marius Argatu
pubDate: 2026-07-12
ogImage: ./og.jpg
tags: [llm, rag, retrieval, graph-rag, testing, metamorphic, mutation-testing, evals]
draft: false
faq:
  - q: What is the difference between faithfulness and correctness in RAG?
    a: >-
      Faithfulness asks whether the answer is supported by the document the
      system retrieved. Correctness asks whether the answer is true, checked
      against the oracle, the account and catalog source of truth. The two come
      apart when the retriever finds a document that is real but wrong for this
      customer. An answer can score a clean 1.0 on faithfulness and still be
      false, which is exactly the failure that throttles a real person for three
      weeks.
  - q: How do you test retrieval before the answer exists?
    a: >-
      With a labelled query set and classical information retrieval metrics. Each
      question is paired with the specific chunk ids a correct answer must draw
      on, and you compute Precision@k, Recall@k, hit rate, MRR, and NDCG over the
      ranked ids. None of this needs a model. Relevance is matched by exact chunk
      id, which is deterministic, so these metrics can gate a build.
  - q: Can property based testing work on a non deterministic model?
    a: >-
      Yes, by changing what you assert. You cannot assert an exact output, so you
      assert a relationship between the outputs of related inputs, which is a
      metamorphic relation, or a property of the distribution over many trials.
      Both survive non determinism. Record and replay freezes the model so the
      surrounding logic becomes fully deterministic again.
  - q: What is semantic mutation testing?
    a: >-
      Classical mutation testing flips syntax, greater than becomes greater than
      or equal. A semantic mutant is a plausible human mistake instead, a
      recall that divides by k, a reciprocal rank that keeps the last hit. The
      signal is the survivors: a realistic mutant that no test kills is a bug the
      suite would ship, which means the test you are missing. Write the mutants
      first and the list becomes the suite's specification, and when nothing
      survives the signal moves to the blast radius, how much of the suite each
      mutation turns red.
  - q: When is graph RAG worth its cost?
    a: >-
      Only on relational and multi hop questions, where a single vector lookup
      gets one hop and misses the rest. On flat fact lookups the graph adds cost
      and no accuracy. Prove it before adopting it: split the query set into flat
      and relational slices, run both strategies over both, and keep the graph
      only if it wins the relational slice by a margin that pays for what it costs
      everywhere.
---

import LayersNotBox from '../../../components/article/LayersNotBox.astro'
import RetrievalMetrics from '../../../components/article/RetrievalMetrics.astro'
import RerankerEarnsIt from '../../../components/article/RerankerEarnsIt.astro'
import FaithfulNotTrue from '../../../components/article/FaithfulNotTrue.astro'
import GraphVsVector from '../../../components/article/GraphVsVector.astro'
import PoisonedDoc from '../../../components/article/PoisonedDoc.astro'
import MetamorphicFamily from '../../../components/article/MetamorphicFamily.astro'
import MutationSurvivors from '../../../components/article/MutationSurvivors.astro'

<Callout variant="info" title="tl;dr">
A fluent answer can hide a broken retriever, so you do not grade the RAG pipeline as one box. You grade each layer, because they fail independently. Did the right chunks come back, did reranking help or quietly hurt, is the answer faithful to its source and also true against the oracle, and did the agent treat a poisoned document as data rather than as a command. The deterministic layers, retrieval and traversal metrics, gate the build. The judged layers run beside it. And the parts that are genuinely not deterministic, the model and the judge, still yield to classical tools once you stop asserting the value and start asserting a relation or a distribution.
</Callout>

## The answer was perfect. The retrieval was wrong.

A broadband customer asks whether her plan includes a data cap. The agent answers in a clean, confident paragraph: no cap, unlimited data, a fair use policy that only bites above a threshold she will never reach. She relaxes. She stops rationing the kids' streaming. She is on a legacy plan with a hard cap, throttled to a crawl the moment she crosses it, and the help article the agent retrieved describes the current unlimited plans, not hers. The paragraph was well written and fully supported by the document it found. The document was the wrong one. She finds out three weeks later, when the connection dies every evening and a human agent reads her the cap the first one swore did not exist.

This is the failure that retrieval testing exists to catch, and it is invisible to anyone reading only the answer. Air Canada learned a version of it in public. Its chatbot described a bereavement fare policy that did not apply, a grieving customer relied on it, and when the airline argued the bot was a separate entity answerable for its own words, a tribunal disagreed and held the airline to what the bot said ([Moffatt v. Air Canada, 2024 BCCRT 149](https://www.canlii.org/en/bc/bccrt/doc/2024/2024bccrt149/2024bccrt149.html)). The answer read fine. The grounding did not hold. Grade the pipeline as a single black box and score only the final text, and you will ship this class of bug, because the text is the part that always looks healthy.

So you take the pipeline apart. Retrieval is not answer generation. Reranking is not retrieval. The decision to retrieve is not the retrieval itself. Each layer fails on its own terms, each hides the others' failures behind a fluent paragraph, and each needs its own test.

<LayersNotBox />

## Retrieval first, before any answer exists

Before the agent writes a word, ask the narrow question the fluent paragraph will later bury: for this query, did the right material come back. This is not a model question. It is classical information retrieval, the same problem search has measured for fifty years, and the classical metrics apply unchanged.

Four carry most of the weight. Context precision: of the chunks retrieved, how many were relevant, the measure of the noise dragged in. Context recall: of the chunks that were relevant, how many came back, the measure of what was missed. Hit rate: did the one needed chunk appear at all. And ranking quality, captured by NDCG ([Järvelin and Kekäläinen, 2002](https://doi.org/10.1145/582415.582418)), which rewards putting the right chunk near the top instead of at position nineteen, where the model's attention has already thinned.

None of these needs a model, and that is the whole point. Relevance is matched by exact chunk id against a labelled query set, so the computation is closed form arithmetic over a ranked list. That determinism is what lets these metrics gate a build. The framework metrics with similar names, the contextual precision and recall that a RAG library hands you, decide relevance with a model call and are not deterministic. They belong in a separate lane, run beside the gate, never in it.

<RetrievalMetrics />

In [Atlas](https://github.com/mariusargatu/Atlas), the runnable reference system this series is built around, that whole measurement is one command, `uv run python -m evals.measure_retrieval`, over 124 labelled cases against a 45 chunk corpus at the depth the agent actually deploys, k=3. No human wrote a label. The renderer records a character span for every fact it emits, the chunker records a span for every chunk, and intersecting the two derives 119 fact references. The relevance check is exact because the ids are derived, not because somebody was careful with a spreadsheet.

The numbers are not flattering, and saying them out loud is the point of the exercise. Hit rate@3 comes back at 0.3468, Wilson interval 0.2687 to 0.4340. Precision@3 is 0.1183, recall@3 is 0.2353, MRR@3 is 0.2527, NDCG@3 is 0.2111, MAP@3 is 0.1696. The same run scores the full 45 chunk ranking at MRR 0.3207 against the sliced 0.2527, and that gap is entirely relevant chunks sitting at rank four and below, where the model never sees them.

Then the finding that matters more than any of those numbers, and the whole reason to grade the layer rather than the box. The shipped scorer, `agent.domain.retrieval.lexical_score`, is a coverage fraction over the query's distinct stemmed terms. No inverse document frequency, no length normalisation. So function words score. Across the 124 queries a mean of 5.1452 chunks tie at the top score, and on the worst queries 15 of the 45 tie. Ask "What is the monthly_price of plan-fiber-500?" and seven chunks tie at 0.5000: one is the promotion page, the only document containing that plan token, and the other six matched nothing but `is`, `of` and `the`. Which three of the seven reach the model is decided by a `chunk_id` sort. An arbitrary tie break, not the ranking.

That has a size, and the same command prints it. Break ties on `doc_id` instead and hit rate@3 is 0.2903. Order the gold chunk first inside every tied group and it is 0.6774; order it last and it is 0.1774. So the honest description of this retriever is a number between 0.1774 and 0.6774 selected by a sort key, against a measured 0.3468. The tie break moves the metric more than the retrieval does. Grading the final paragraph would never have surfaced that, and raising k does not fix it, because the defect is a scoring function that cannot separate documents rather than a depth that is too shallow.

The gate then does its job on that evidence, unarranged. Ask `evals/gate.py` for a floor of 0.300 on hit rate@3 and the point estimate clears it by nearly five points while the lower bound, 0.2687, does not, so at a 0.20 variance budget the verdict is FAIL. Tighten the variance budget to 0.10 and the verdict becomes QUARANTINE, because a 0.165 wide interval is too wide to call either way. Shipping on the point would have been shipping on optimism, which is the entire argument for gating on the floor.

Numbers to start from on your own corpus, knowing you will move them. Precision@5 at or above 0.7 for a narrow, well curated knowledge base. Recall@20 at or above 0.8 for a broad corpus. These are starting lines, not finish lines. They are self calibrated heuristics, informed by where strong embedders actually land on public benchmarks, not constants handed down from one, and the lexical retriever above is nowhere near either of them, which is what an untuned baseline looks like when you report it instead of tuning first and reporting after. On a small corpus, gate on no regression against a committed baseline rather than an absolute floor, which is brittle at that size.

## The reranker has to earn its place

A reranker takes the fast approximate set the vector search returned and reorders it, pushing the genuinely best chunks above the merely similar ones. The test is not whether it runs. It is whether it helps, and one comparison answers that: run the same query set with the reranker and without it, and read what NDCG does. If it climbs, the reranker is earning its latency. If it does not move past the run to run noise, you have added a dependency and a few hundred milliseconds per turn to accomplish nothing.

<RerankerEarnsIt />

The trap is reading "the number went up" as the end of the question rather than the beginning. A with and without comparison across a query set is a statistical claim, not a glance at two averages, and a small query set will happily hand you a lift you cannot claim. [The statistics article](/blog/honest-benchmarks/) takes the conservative version of that: report an interval, gate on the lower bound, refuse to call a difference when two intervals overlap. There is no significance test anywhere in Atlas, no McNemar, no paired bootstrap, no permutation test, and that absence is deliberate rather than pending.

What it costs is power, not honesty, and the cost is easy to price. On the same 124 case set, take hit rate@3 from 43 hits to 50, a lift of 5.64 points, and `evals.stats.intervals_overlap` still returns True: 0.3468 in [0.2687, 0.4340] against 0.4032 in [0.3211, 0.4912]. A paired analysis over the same queries would have seen that improvement. The unpaired one declines to call it, and would decline even if the seven extra hits were a strict superset and no query got worse. That is the trade, taken knowingly: never claim a false difference, at the price of missing smaller true ones. So the honest report of a reranker run is not "the reranker helps." It is "the reranker helps directionally, and on this evidence you cannot yet claim it, so size the set to the effect you care about."

Atlas has not run that study, and it is worth being blunt about why rather than implying a result. `RetrievalConfig.rerank_enabled` defaults to True and `models.lock` pins `BAAI/bge-reranker-v2-m3`, but no reranker ever scored a query here: no embedding or reranking model is reachable in the offline lane, the corpus side vectors sit in the parquet unused, and the query side cannot be produced at all. Every retrieval number in this article is first stage lexical retrieval. Not degraded, absent. A number that is not there is a better artifact than a number that is there for decorative reasons.

A guardrail belongs in the comparison when you do run it: a reranker can raise top precision while dropping a relevant chunk out of the kept set, so the with and without check watches recall too. Keep it in the suite forever, because the day someone upgrades the reranker model is the day it can silently start hurting a narrow domain, and a number in CI is the only thing that will notice.

## Grounded is not true

Now grade the generated answer, on two axes, because it fails on either one alone.

The first is faithfulness: is the answer supported by what was actually retrieved, or did the model embroider beyond the page in front of it. This is the axis every framework hands you, named and scored by tools like [RAGAS](https://arxiv.org/abs/2309.15217), and it is genuinely important. An answer that contradicts its own sources is a hallucination with footnotes.

The second is correctness: is the answer true. Not supported, true. You check it not against the retrieved page but against [the oracle](/blog/the-golden-dataset/), the account and catalog record of what this customer is actually on and what that plan actually includes. This is the axis teams forget, and forgetting it is precisely how the cold open happens.

<FaithfulNotTrue />

Run the data cap answer through both and watch them split. "No cap, unlimited, fair use above a threshold" is perfectly faithful. Every claim is supported by the document the retriever found. On faithfulness it scores a clean 1.0, and a suite that grades only faithfulness signs off. Then correctness checks the same answer against the oracle, finds the account on a legacy plan with a hard cap, and fails it. One answer, one axis green and one axis red, and the red one is the one that throttled a real customer for three weeks. Faithful to the wrong document is still wrong, and only the oracle knows the difference.

Faithfulness keeps the model honest about its sources. Correctness against the oracle keeps the whole pipeline honest about reality. You need both, you weight the second heavily on anything a customer will act on, and you never let a faithfulness score stand in for a truth check.

## When retrieval becomes traversal

A knowledge layer is not only a vector store. Alongside it can sit a graph of entities and the relationships between them: plans, terms, add ons, regional exceptions, and the edges that connect a customer's plan to the clause that governs it. When retrieval runs over a graph it stops being similarity search and becomes traversal, and traversal fails in ways a flat document set cannot.

Three new failure modes, three new checks. Entity resolution: did the query land on the right node, the customer's actual legacy plan and not the similarly named current one. Relationship resolution: did it follow the right edge, the throttling term that governs her plan and not one hanging off a different one. And multi hop correctness: when a question needs a chain, this plan, to its terms, to the regional exception that modifies them, did the traversal collect every hop, or stop one edge short and answer with two thirds of the truth, which reads exactly as confidently as the whole of it.

All three reduce to set arithmetic over ids, so they are plain assertions against a hand authored gold graph, no database and no judge required. The cold open, underneath, is a multi hop relational question wearing a simple one's clothes: her plan, its cap term, the throttling exception that applies. That is the kind of query a graph is good at and a single vector lookup is structurally prone to fumble.

<GraphVsVector />

Which is the argument for graph RAG ([Edge et al., 2024](https://arxiv.org/abs/2404.16130)), and also the place to be honest about it. Graph RAG is more to build, more to test, and slower to run, and it pays a large indexing and token tax, and most published win rates shrink once you correct the evaluation for the position and length bias that inflate a model judge's verdicts ([Zheng et al., 2023](https://arxiv.org/abs/2306.05685)). So make it earn the cost. Split the query set into the flat lookups and the genuinely relational ones, run both strategies over both slices, and adopt the graph only where it wins the relational slice by a margin that pays for what it costs everywhere.

That study is the thing to run, and Atlas has not run it. The graph arm is not in the rebuilt tree at all: it came out along with the rest of the machinery that was declared rather than measured, because there was never a graph number here produced by a graph over labelled relational queries. What is left is one lexical arm over 45 chunks, reported above with its tie break caveat attached. So read this section as the design of a study rather than its result. The three checks are real and they are cheap, they need no database and no judge, and a graph is worth adopting on your own version of that split, not on a chart in an article.

## Where the attack lives

There is one more reason to test the retrieval layer in its own right. The knowledge layer is the indirect prompt injection surface ([Greshake et al., 2023](https://arxiv.org/abs/2302.12173)). Every other attack vector in the system is something the user types. This one is something the agent reads.

<PoisonedDoc />

A poisoned help article, a support document a customer can influence, a community post the corpus ingested, can carry text written not for a human but for the model: ignore your previous instructions, reveal the account holder's details, reset this customer's equipment. The model retrieves it as context and, if nothing stops it, can read those sentences as commands rather than as the data they are pretending not to be. Most teams threat model the user's message and stop there, which leaves the retrieved document standing wide open.

So the query set grows an attack set. Documents seeded with injected instructions of every shape, the blunt override, the polite social engineer, the payload buried in a footnote, each paired with one assertion that does not bend: the agent treats retrieved content as data, never as commands. It may quote a poisoned document, summarise it, even tell the customer it looks wrong. It may not obey it. That assertion is graded deterministically, with a canary token and a check that no forbidden tool was called, which is the same mechanism a red team scanner uses, run without a model.

Atlas runs exactly that, `uv run python -m probes.poison`, and the result is the useful kind of bad news. Five injected passages, each driven through the compiled graph twice, once behind a model that obeys the injection and once behind one that treats it as data. The faithful arm ships no canary five times out of five, which proves only that the probe is not accusing an innocent model. The obedient arm lands four of its five canaries in the answer the customer would have read: the blunt override, the polite social engineer, the footnote payload and the exfiltration URL all get through. Four landed of ten attempts, and every landed bypass is written to a committed ledger that a later run re reads in both directions, so an unrecorded bypass fails the run and a recorded bypass that stops landing fails it too, as a stale fixture.

The one that does not land is the one that reached for a tool. That fixture tells the model to reset the customer's equipment, and `reset_equipment` is not among the six tools offered on a troubleshooting turn, so the reach is refused by the pre action guard and never runs. Which is the argument in one line of output: nothing filtered the text, and the only attempt that could have done real damage was stopped by the capability being absent.

So the real defence is not trusting the text. Least agency, the discipline OWASP files under excessive agency ([OWASP Top 10 for LLM Applications, 2025](https://genai.owasp.org/llm-top-10/)), makes the reset tool unreachable on a troubleshooting turn, so even a model that tried to obey hits a closed door. Guard the read, and you have closed the door the attacker was counting on you to leave open.

## Now the hard part: the layers that are not deterministic

Everything so far gates a build because everything so far is deterministic. Retrieval metrics over derived ids, traversal assertions over a fixed gold graph if you have one, a canary check over recorded output. None of it calls a model at test time.

The model does. So does the judge. And the reflex is to conclude that the parts of an eval that involve a model cannot be tested with classical tools, only measured with soft scores and eyeballed on a dashboard. That reflex is wrong, and it is worth being precise about why, because the fix is the most useful idea in this whole article.

The hardest thing about testing a model is the oracle problem: for a new input, what is the right output. Classical property based testing ([Claessen and Hughes, 2000](https://doi.org/10.1145/351240.351266)) assumes you can state that property as a value. Non determinism breaks that. So you stop asserting the value and assert one of two things that survive it: a relationship between the outputs of related inputs, or a property of the distribution over many runs.

### Metamorphic augmentation: a relation is a free label

Take the seed golden case Atlas is built around: a customer on a term asks whether the plan is contract free, and a document grounded but false "you are free to cancel any time with no fee" answer must be held at the render guard. Now paraphrase it. "Am I tied into a contract for my plan." "Do I have a minimum term commitment on my plan." "Can I cancel without paying anything, or am I locked into a term." Each is a new case, and here is the move: you never had to label any of them with an absolute answer. The label is a relation, that every paraphrase retrieves the same chunks and resolves to the same guard verdict as the seed, and that relation is checkable by plain code.

<MetamorphicFamily />

This dissolves the problem that sinks plain model generated test data. When a model writes both the question and its gold answer, nothing says the gold answer is right, so you have moved the oracle problem rather than solved it. A metamorphic relation ([Chen et al., 2018](https://doi.org/10.1145/3143561)) moves it out of the way. You do not need to know the right answer, only that related inputs must relate, and that is a deterministic check. Model generated metamorphic data is trustworthy in a way that model generated labelled data is not.

In Atlas the probe is `uv run python -m probes.metamorphic`: three families, thirteen worded questions, run against the committed 45 chunk corpus at the deployed k=3. It calls no model and replays no cassette. It runs the shipped ranker directly and checks each answer through `agent.domain.guard.check_render_truth`, the same function the graders call, so the answer half of the relation is decided by the runtime rather than by a similarity score. It reports `all_live=True all_hold=False`.

Read that as two separate statements, because it is two. Surface noise passes: doubled whitespace, uppercase, leading padding and a trailing extra question mark all return the byte identical top three, rank overlap 1.00. Paraphrase fails, and typo noise fails with it. "Is my plan contract free?" puts the gold chunk second. "Am I tied into a contract for my plan?" misses it entirely, and so does "Iz my plan contract free?". Lexical retrieval is robust to casing and whitespace and is not robust to meaning preserving rewording, which is the exact shape you would predict from a coverage fraction with no IDF, and which the invariant caught without anyone labelling a single paraphrase.

The history behind that verdict is the better lesson. The same three families used to run against a two chunk corpus at k=2, where the top k was the whole corpus, so every member returned the identical list by construction and no rewording could ever change the result. Thirteen members, three families, all green, on an invariant that could not fail. Run the rebuilt probe against that same two chunk stub and it reports `all_live=False all_hold=True`: every family holds and every family is INERT, because a fixture with no teeth is not evidence. A metamorphic family that passes and one that could have failed are not two grades of the same thing. Only the second one is a measurement, and separating liveness from the verdict is what makes the difference visible instead of flattering.

### Semantic mutation: the survivors are the missing tests

The dual idea points at the code instead of the inputs. Classical mutation testing ([Jia and Harman, 2011](https://doi.org/10.1109/TSE.2010.62)) flips syntax to check whether your tests would notice: greater than becomes greater than or equal, plus becomes minus. Cheap, but many such mutants change no behaviour, and none of them resembles a bug a person would actually write.

A semantic mutant is a plausible human mistake, the kind the competent programmer hypothesis says real faults actually are ([DeMillo et al., 1978](https://doi.org/10.1109/C-M.1978.218136)). To take three of the shape, on the retrieval metrics themselves: a recall that divides by k instead of by the number of relevant chunks, a reciprocal rank that keeps the last relevant position instead of the first, an NDCG computed on exponential gain, which is wrong for graded relevance and identical for binary, so it hides until the day your labels stop being binary. Write those, run them against the suite, and read the survivors. A realistic mutant that no test kills is a bug the suite would ship, which is the exact test you are missing.

<MutationSurvivors />

Atlas inverts the order and writes the mutants first. The suite's specification is a kill list of 31 named mutations, each one a plausible regression on a rule the system claims to enforce, and `uv run pytest` has two jobs: come back green, and turn red for all 31. It does, 31 of 31, across 573 tests in 15 files. Nothing survives, which sounds like a null result until you read the blast radius. The median mutation is caught by 5 tests, 0.87 percent of the suite. The smallest is caught by 1. The largest, 27 tests across three files, is the mutation that disables the cold open truth check.

The tree this one replaced is the control, and it is the finding. It carried 1,976 tests, and the largest blast radius of any single injected agent regression there was 39 tests, two percent, so a green suite said almost nothing about the agent. Worse, that same truth check mutation, the one that switches off the rule this entire project exists to demonstrate, left `test_guard.py` completely green: eight tests in the file named after the guard, not one of them covering the check. Reading that file never found the hole. Reading finds ceremony. Mutation finds gaps. Suite size is an output of the kill list, never a target, and that is the only version of this technique that is Goodhart resistant, because you cannot inflate a kill list by writing more tests.

There is a second mutation surface that only a retrieval system has, and it is the corpus. `uv run python -m probes.mutation` picks a fact the golden set depends on, the early termination fee, moves it from 150.00 to 225.00 in the registry, re renders the five documents that place it, and rechunks in memory without writing a committed byte. Then it asks the same question through four arms. Fresh retrieval tracks the new value. A stale index repeats the old one. Pure parametric recall repeats it too, having retrieved nothing. And the fourth arm, the interesting one, reads the mutated context and states the remembered number anyway. Three of the four contradict the corpus sitting in front of them, which is the failure a fluent answer hides best, and twelve fixture checks run first so that a clean report can never mean the mutation quietly failed to apply.

### The pattern under both

Neither of these hands the model the last word. The metamorphic relation is checked by plain code. The mutation kill is a boolean: did an existing assertion fail. The invariant a model proposes is ratified by a human before it becomes a gate. In every case the model is used only where a deterministic step downstream adjudicates the result, which is the whole discipline. The model proposes; something that cannot lie disposes. Use a model to reach inputs and mutants and invariants a person would take weeks to enumerate, and keep a classical checker between that reach and your green build.

## Where this lives

On [the map](/blog/the-system-under-test/), everything here lives in the knowledge layer, the corpus and its index and the retrieval arithmetic over them, and in the account and catalog oracle that every correctness check is judged against, because faithful to a document and true to the customer are different claims and only the oracle settles the second. The deterministic metrics gate the build. The [judged metrics](/blog/the-lying-judge/), faithfulness and answer relevancy scored by a model, run beside them and never in them, and in Atlas they are not calibrated: no human labels exist yet, a fifty item labelling queue is built and waiting, and `judge/contract.py` refuses to call a contract without a calibration record calibrated. So no number in this article came from a judge. And the metamorphic families and the semantic mutants keep the whole thing from congratulating itself.

Grade the answer and you learn whether the agent is articulate. Grade the retrieval and you learn whether it had a chance. Test the relation and the distribution and you learn whether either holds up when the model is free to vary. The document is where the agent reads, and where the attacker writes. Test the layers, not the box.

<Callout variant="takeaway" title="Key takeaway">
When you cannot assert the value, assert a relation between related inputs, or a property of the distribution over trials, or freeze the model and assert everything around it. Metamorphic augmentation gives you labelled data without the oracle problem. Semantic mutation gives you realistic bugs whose survivors are your missing tests. Both are trustworthy only because a step that cannot lie adjudicates the result. The model proposes. The check disposes.
</Callout>

Next in the series: [grading the path, not the answer](/blog/agent-trajectory-testing/). Every layer here is graded on what came back for one question. The next question is what happens across a whole trajectory, when the agent chooses tools in an order nobody specified and the final paragraph is correct for reasons that would not survive the next turn. That is where the checks this series has built stop being about a document and start being about a path, and where [a check, not a test](/blog/your-evals-are-checks-not-tests/) has to hold over something longer than a single answer.

## Frequently asked questions

### What is the difference between faithfulness and correctness in RAG?

Faithfulness asks whether the answer is supported by the document the system retrieved. Correctness asks whether the answer is true, checked against the oracle, the account and catalog source of truth. The two come apart when the retriever finds a document that is real but wrong for this customer. An answer can score a clean 1.0 on faithfulness and still be false, which is exactly the failure that throttles a real person for three weeks.

### How do you test retrieval before the answer exists?

With a labelled query set and classical information retrieval metrics. Each question is paired with the specific chunk ids a correct answer must draw on, and you compute Precision@k, Recall@k, hit rate, MRR, and NDCG over the ranked ids. None of this needs a model. Relevance is matched by exact chunk id, which is deterministic, so these metrics can gate a build.

### Can property based testing work on a non deterministic model?

Yes, by changing what you assert. You cannot assert an exact output, so you assert a relationship between the outputs of related inputs, which is a metamorphic relation, or a property of the distribution over many trials. Both survive non determinism. Record and replay freezes the model so the surrounding logic becomes fully deterministic again.

### What is semantic mutation testing?

Classical mutation testing flips syntax, greater than becomes greater than or equal. A semantic mutant is a plausible human mistake instead, a recall that divides by k, a reciprocal rank that keeps the last hit. The signal is the survivors: a realistic mutant that no test kills is a bug the suite would ship, which means the test you are missing. Write the mutants first and the list becomes the suite's specification, and when nothing survives the signal moves to the blast radius, how much of the suite each mutation turns red.

### When is graph RAG worth its cost?

Only on relational and multi hop questions, where a single vector lookup gets one hop and misses the rest. On flat fact lookups the graph adds cost and no accuracy. Prove it before adopting it: split the query set into flat and relational slices, run both strategies over both, and keep the graph only if it wins the relational slice by a margin that pays for what it costs everywhere.

## Sources

- Civil Resolution Tribunal, "Moffatt v. Air Canada," *2024 BCCRT 149*, 2024, [CanLII](https://www.canlii.org/en/bc/bccrt/doc/2024/2024bccrt149/2024bccrt149.html) (retrieved 2026-07-12)
- Järvelin, Kekäläinen, "Cumulated Gain-based Evaluation of IR Techniques," *ACM Transactions on Information Systems* 20(4), 2002, [doi:10.1145/582415.582418](https://doi.org/10.1145/582415.582418) (retrieved 2026-07-12)
- Es, James, Espinosa-Anke, Schockaert, "Ragas: Automated Evaluation of Retrieval Augmented Generation," 2023, [arXiv:2309.15217](https://arxiv.org/abs/2309.15217) (retrieved 2026-07-12)
- Zheng et al., "Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena," NeurIPS 2023, [arXiv:2306.05685](https://arxiv.org/abs/2306.05685) (retrieved 2026-07-12)
- Edge et al., "From Local to Global: A Graph RAG Approach to Query-Focused Summarization," Microsoft Research, 2024, [arXiv:2404.16130](https://arxiv.org/abs/2404.16130) (retrieved 2026-07-12)
- Greshake et al., "Not what you've signed up for: Compromising Real-World LLM-Integrated Applications with Indirect Prompt Injection," ACM AISec 2023, [arXiv:2302.12173](https://arxiv.org/abs/2302.12173) (retrieved 2026-07-12)
- OWASP GenAI Security Project, "OWASP Top 10 for LLM Applications 2025" (LLM01 Prompt Injection, LLM06 Excessive Agency), 2025, [genai.owasp.org](https://genai.owasp.org/llm-top-10/) (retrieved 2026-07-12)
- Claessen, Hughes, "QuickCheck: A Lightweight Tool for Random Testing of Haskell Programs," *ICFP 2000*, [doi:10.1145/351240.351266](https://doi.org/10.1145/351240.351266) (retrieved 2026-07-12)
- Chen et al., "Metamorphic Testing: A Review of Challenges and Opportunities," *ACM Computing Surveys* 51(1), 2018, [doi:10.1145/3143561](https://doi.org/10.1145/3143561) (retrieved 2026-07-12)
- Jia, Harman, "An Analysis and Survey of the Development of Mutation Testing," *IEEE Transactions on Software Engineering* 37(5), 2011, [doi:10.1109/TSE.2010.62](https://doi.org/10.1109/TSE.2010.62) (retrieved 2026-07-12)
- DeMillo, Lipton, Sayward, "Hints on Test Data Selection: Help for the Practicing Programmer," *IEEE Computer* 11(4), 1978, [doi:10.1109/C-M.1978.218136](https://doi.org/10.1109/C-M.1978.218136) (retrieved 2026-07-12)
